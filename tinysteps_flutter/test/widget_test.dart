// Basic Flutter widget test for TinySteps AI app.

import 'package:flutter_test/flutter_test.dart';
import 'package:tinysteps_ai/main.dart';

void main() {
  testWidgets('TinyStepsApp is defined and can be referenced', (WidgetTester tester) async {
    // Verify the app class exists and is accessible
    expect(TinyStepsApp, isNotNull);
  });
}
